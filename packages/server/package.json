{
  "name": "@roughdraft/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @roughdraft/rfm build && rm -rf dist && tsc",
    "dev": "tsx watch --conditions=development src/dev.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@roughdraft/rfm": "workspace:*",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.4.0",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
